name: go-do

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: setup
        uses: actions/setup-go@v3
        with:
          go-version: 1.17.8

      - name: test
        run: go test -v ./...